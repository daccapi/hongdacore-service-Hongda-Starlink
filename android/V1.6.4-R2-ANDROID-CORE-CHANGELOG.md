# V1.6.4 R2 Android Core Bridge

- Synced Windows shared business baseline to V1.6.4-R2 RegressionFix.
- Pinned vendored sing-box source to v1.13.18 / 45ca32dcb966f07f97fc888fe8586e359dbe8405.
- Added custom `build_hongdacore` gomobile target:
  - `HongdaCore.aar`
  - `-javapkg=com.hongda.starlink.core`
  - `-libname=hongdacore`
  - native `libhongdacore.so`
- Replaced placeholder Android VpnService shell with real `CommandServer`/`PlatformInterface` bridge.
- Added real Android TUN creation, socket protect, default-interface monitoring, Android CA store, VPN lifecycle and foreground service.
- MainActivity now starts/stops the real VPN service and waits for real core READY/error state.
- Android traffic/connection metrics now reuse Clash API `/traffic` and `/connections` instead of native zero placeholders.
- Android connected node switching now mirrors Windows R2: hot selector switch first, controlled core reload fallback.
- Synced R2 TUN/DNS 172.19 peer regression fix in the shared config builder.
- Added one-click Windows build helpers for Android Core and arm64 APK.
